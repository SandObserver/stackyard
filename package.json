{
  "name": "stackyard-dev",
  "private": true,
  "description": "Dev-only tooling (lint + typecheck). Not shipped: the Docker image copies only api/ and ui/, never the repo root.",
  "scripts": {
    "lint": "biome lint .",
    "typecheck": "tsc -p tsconfig.json",
    "typecheck:ui": "tsc -p tsconfig.frontend.json"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.2",
    "@types/node": "26.1.0",
    "typescript": "6.0.3"
  }
}
