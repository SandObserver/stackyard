{
  "name": "dns",
  "label": "DNS Server",
  "sizes": ["small", "medium"],
  "fields": [
    { "key": "provider", "type": "select", "variant": "pills", "label": "Provider",
      "options": [
        { "value": "adguard", "label": "AdGuard Home" },
        { "value": "pihole",  "label": "Pi-hole" }
      ],
      "hint": "Which DNS sinkhole this widget reads from." },

    { "key": "dnsUrl",  "type": "text", "label": "Server URL", "placeholder": "dns-host:80",
      "hint": "Container name and port, or a full URL." },
    { "key": "dnsHref", "type": "text", "label": "Click URL", "optional": true,
      "hint": "Where clicking the widget opens. Leave blank to disable." },

    { "key": "dnsUser", "type": "text",   "label": "Username", "optional": true,
      "showIf": { "field": "provider", "equals": "adguard" } },
    { "key": "dnsPass", "type": "secret", "label": "Password", "optional": true,
      "showIf": { "field": "provider", "equals": "adguard" } },

    { "key": "dnsPiholePassword", "type": "secret", "label": "Pi-hole Password", "optional": true,
      "showIf": { "field": "provider", "equals": "pihole" },
      "hint": "Leave blank if your Pi-hole has no password set." }
  ]
}
