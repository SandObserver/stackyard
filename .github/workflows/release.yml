name: Build and Push Docker Image

on:
  push:
    tags:
      - 'v*'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      # Keyless signing exchanges this for a short-lived Sigstore certificate.
      # No key material is stored anywhere.
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          persist-credentials: false

      # Runs every check the Tests workflow runs, then stamps the cache-busting
      # versions, so a tag cannot publish a tree that would fail CI. A tag used
      # to go straight from checkout to push: no tests, no lint, no typecheck,
      # and a mutating stamp pass whose output nothing verified.
      #
      # Failing here leaves the tag in place with nothing published. Fix main,
      # delete the tag, tag again.
      - name: Run project checks
        uses: ./.github/actions/checks
        with:
          mode: release

      - name: Set up QEMU
        uses: docker/setup-qemu-action@c7c53464625b32c7a7e944ae62b3e17d2b600130 # v3.7.0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3.12.0

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.7.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Docker Hub is a mirror, and entirely optional: with no credentials set
      # the release publishes to ghcr.io exactly as before.
      #
      # One step decides, and both the login and the tag list read its answer.
      # Deciding twice, once in an `if:` and once here, is how a build ends up
      # pushing to a registry it never logged in to.
      - name: Choose registries
        id: registries
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_NAMESPACE: ${{ vars.DOCKERHUB_NAMESPACE }}
        run: |
          if [ -n "$DOCKERHUB_USERNAME" ]; then
            echo 'dockerhub=true' >> "$GITHUB_OUTPUT"
          else
            echo 'dockerhub=false' >> "$GITHUB_OUTPUT"
            echo 'No Docker Hub credentials set; publishing to ghcr.io only.'
          fi
          {
            echo 'images<<REGISTRY_LIST'
            echo 'ghcr.io/sandobserver/stackyard'
            if [ -n "$DOCKERHUB_USERNAME" ]; then
              echo "docker.io/${DOCKERHUB_NAMESPACE:-$DOCKERHUB_USERNAME}/stackyard"
            fi
            echo REGISTRY_LIST
          } >> "$GITHUB_OUTPUT"

      - name: Log in to Docker Hub
        if: steps.registries.outputs.dockerhub == 'true'
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.7.0
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # A hyphen in the ref used to decide this. See scripts/is-prerelease.js:
      # build metadata can contain a hyphen, and a mistyped tag has none, which
      # is the case that quietly moved `latest`.
      - name: Classify the tag
        id: tag
        run: node scripts/is-prerelease.js "$GITHUB_REF_NAME" >> "$GITHUB_OUTPUT"
        env:
          GITHUB_REF_NAME: ${{ github.ref_name }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@c299e40c65443455700f0fdfc63efafe5b349051 # v5.10.0
        with:
          images: ${{ steps.registries.outputs.images }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable=${{ steps.tag.outputs.prerelease == 'false' }}

      - name: Build and push
        id: build
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6.19.2
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          build-args: |
            APP_VERSION=${{ steps.meta.outputs.version }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      # Everything below addresses the image by digest, not by tag. A tag can be
      # moved; the digest is the thing that was built, scanned and signed.

      # Scanning happens after the push because the multi-platform image is
      # assembled by buildx and never exists locally. A finding therefore fails
      # the release with the image already in the registry: the tag is published
      # but the run is red, and the recovery is the same as any other failed
      # release, fix main and cut a new tag.
      - name: Scan the image
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: ghcr.io/sandobserver/stackyard@${{ steps.build.outputs.digest }}
          format: table
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: '1'

      - name: Install cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2

      # Keyless: the signature is bound to this workflow's identity through the
      # OIDC token above, and recorded in the public transparency log. There is
      # no private key to hold or rotate.
      #
      # Verify a release with:
      #   cosign verify ghcr.io/sandobserver/stackyard:<tag> \
      #     --certificate-identity-regexp '^https://github.com/SandObserver/stackyard/' \
      #     --certificate-oidc-issuer https://token.actions.githubusercontent.com
      - name: Sign the image
        run: cosign sign --yes "ghcr.io/sandobserver/stackyard@${DIGEST}"
        env:
          DIGEST: ${{ steps.build.outputs.digest }}

      - name: Generate SBOM
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0
        with:
          image: ghcr.io/sandobserver/stackyard@${{ steps.build.outputs.digest }}
          format: spdx-json
          output-file: stackyard-${{ steps.meta.outputs.version }}.spdx.json
          upload-artifact: false
          upload-release-assets: false

      - name: Upload SBOM
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: sbom-${{ steps.meta.outputs.version }}
          path: stackyard-${{ steps.meta.outputs.version }}.spdx.json
          if-no-files-found: error
